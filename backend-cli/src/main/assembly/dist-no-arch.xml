<?xml version="1.0" encoding="UTF-8"?>
<!--    Generate a distribution archive which contains executable application with dependencies and supporting files
        - The actual jar
        - the dependencies
        - the launcher scripts
          -->
<assembly>
  <id>no-arch</id>
  <includeBaseDirectory>false</includeBaseDirectory>
  <formats>
    <!-- ZIP archive as Windows cannot work with tar.gz out of the box but Unix can work with zip out of the box  -->
    <format>zip</format>
  </formats>

  <!-- The dependencies -->
  <dependencySets>
    <dependencySet>
      <outputDirectory>/lib</outputDirectory>
      <unpack>false</unpack>
      <excludes>
        <exclude>${artifact}</exclude>
      </excludes>
    </dependencySet>
  </dependencySets>

  <fileSets>
    <fileSet>
      <!-- The sloop jar-->
      <directory>${project.build.directory}</directory>
      <outputDirectory>lib</outputDirectory>
      <fileMode>0644</fileMode>
      <includes>
        <include>**/${project.artifactId}-${project.version}.jar</include>
      </includes>
    </fileSet>

    <!-- The launcher scripts (we don't know the environment, so package both and let the client decide) -->
    <fileSet>
      <directory>src/main/assembly/bin</directory>
      <outputDirectory>bin</outputDirectory>
      <fileMode>0755</fileMode>
      <lineEnding>unix</lineEnding>
      <filtered>true</filtered>
      <includes>
        <include>sonarlint-backend</include>
      </includes>
    </fileSet>

    <fileSet>
      <directory>src/main/assembly/bin</directory>
      <outputDirectory>bin</outputDirectory>
      <fileMode>0755</fileMode>
      <lineEnding>dos</lineEnding>
      <filtered>true</filtered>
      <includes>
        <include>sonarlint-backend.bat</include>
      </includes>
    </fileSet>
  </fileSets>
</assembly>
